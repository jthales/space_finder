import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'space_finder',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
